apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: eks-demo-hpa
  namespace: app-demo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eks-demo-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50